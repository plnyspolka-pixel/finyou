// Wzory dokumentów wycofane na rzecz silnika umów (use_case = 'legacy' w
// document_templates). Jedno źródło prawdy dla umowy pożyczki to
// src/lib/contract-engine — kreator /admin/kreator-pozyczki, agent umowy, MCP.

export const LEGACY_USE_CASE = "legacy";

export const LEGACY_KOMUNIKAT =
  "Ten wzór jest wycofany (legacy). Umowę pożyczki generuje kreator pożyczki z silnika umów — /admin/kreator-pozyczki.";
